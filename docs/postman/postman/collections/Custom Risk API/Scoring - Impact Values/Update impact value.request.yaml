$kind: http-request
url: "{{baseUrl}}/api/v1/registers/{{registerId}}/impact-values/{{impactId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Major",
      "numericValue": 4,
      "displayOrder": 4,
      "isActive": true
    }
order: 3000
