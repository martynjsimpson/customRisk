$kind: http-request
description: Optional MVP route listed in the route map.
url: "{{baseUrl}}/api/v1/registers/{{registerId}}/exports"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "filters": {
        "state": "OPEN",
        "overdue": true,
        "includeClosed": false,
        "search": "vendor"
      }
    }
order: 2000
