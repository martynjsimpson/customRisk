$kind: http-request
description: Searches person references and linked users for picker/autocomplete flows.
url: "{{baseUrl}}/api/v1/persons/search"
method: GET
queryParams:
  - key: q
    value: ali
  - key: limit
    value: "10"
order: 1000
