$kind: http-request
url: "{{baseUrl}}/api/v1/registers/{{registerId}}/permissions"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "userId": "{{userId}}",
      "role": "REGISTER_ADMIN"
    }
order: 2000
