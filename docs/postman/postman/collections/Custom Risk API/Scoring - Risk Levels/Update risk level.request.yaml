$kind: http-request
url: "{{baseUrl}}/api/v1/registers/{{registerId}}/risk-levels/{{riskLevelId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "High",
      "description": "Requires active management attention.",
      "displayOrder": 3,
      "isActive": true
    }
order: 3000
