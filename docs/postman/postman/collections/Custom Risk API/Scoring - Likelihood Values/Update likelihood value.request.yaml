$kind: http-request
url: "{{baseUrl}}/api/v1/registers/{{registerId}}/likelihood-values/{{likelihoodId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Possible",
      "numericValue": 3,
      "displayOrder": 3,
      "isActive": true
    }
order: 3000
