$kind: http-request
description: Requires the `draftConfig` feature. Discards the current draft configuration version.
url: "{{baseUrl}}/api/v1/registers/{{registerId}}/config-versions/draft"
method: DELETE
order: 1400
