# Custom Risk — MVP API Route Map

**Version:** 1.0  
**Date:** 2026-05-09  
**Status:** Archived  
**Applies to:** Historical MVP design reference only

---

## 1. Status

This document no longer serves as the active source of truth for the Custom Risk
API surface.

It originally captured the planned MVP endpoint inventory, but the product has
moved beyond MVP and the route-level details in this file are no longer the best
maintenance point.

---

## 2. Current Sources of Truth

Use these documents instead:

- `docs/architecture/api-standards.md` for API-wide conventions such as base path, response envelopes, error codes, pagination, sorting, and route design rules.
- `docs/postman/README.md` and the Postman collection under `docs/postman/` for the current implemented endpoint surface and example requests.
- `docs/architecture/permission-model.md`, `docs/architecture/audit-model.md`, and `docs/architecture/security-model.md` for cross-cutting backend policy rules.

---

## 3. Historical Note

Keep this file path reserved so older planning and architecture references do
not break unexpectedly. If historical MVP route-planning detail is ever needed,
recover it from Git history rather than treating this file as current guidance.
